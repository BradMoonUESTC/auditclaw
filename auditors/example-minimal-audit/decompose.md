Read the project and split it into a small set of audit tasks.
Write each task as `audit-materials/decompose/tasks/<task_id>/task.json`.
Each task file must include at least `task_id`, `title`, and `scope`.
